# Handoff: Structural Refactor — 2026-09-14

Goal: one canonical file per purpose, no duplicates, no dead files, docs match reality.
No resume substance changed — only moves/deletes of stale or byte-identical files plus doc updates.

## versions/ — one base per track (pipeline Stage 06 contract)
- `startup/_base.md` synced from `resume_startup.md` (hash-verified identical), then `resume_startup.md` deleted. The old `_base.md` was stale (missing #8918 grounding + #8991/#12360 specs); the pipeline was reading the stale copy.
- `mnc/resume_mnc.md` → `archive/resume_mnc.md.stale-20260914` (generic bullets, fully subsumed by `_base.md`, zero references).
- `midlevel/resume_midlevel.md` → `archive/resume_midlevel.md.stale-20260914` (missing grounding/specs, subsumed by `_base.md`, zero references).
- Kept tailored outputs: `startup/resume_noise.md`, `startup/resume_Noise_PM.md` (user's open file), `mnc/resume_crisil_ai_intern.md`.
- Rule: canonical = `_base.md`. Tailored = `resume_{Company}.md`. Never both for the same content.

## data/ — boundaries.yaml quarantined
- `data/boundaries.yaml` → `archive/boundaries.yaml.unreferenced-20260914`. Nothing in the workspace references it, and its header ("Stage 07 reads this file directly") is false — Stage 07 reads `content/github/boundaries.md`. Truth boundaries live in `data/facts.yaml` (summary) + `content/github/boundaries.md` (enforced).

## templates/ — junk removed, variants documented
- Deleted regenerable build output: `1/{resume.aux,resume.log,resume.out}`, `2/{resume.aux,resume.log,resume.out}`.
- Deleted `2.zip` (755KB exact copy of the `2/` folder including its build junk; re-zippable any time).
- Kept `1/` + `2/` (same `.sty`, different section content/title style) + `1/original_backup/` (restore point) + PDF/PNG previews.
- New `templates/README.md` records which is which + build command.

## assets/ — exact duplicates deleted, superseded drafts archived
- Deleted `assets/sections/` (5 files byte-identical to `templates/1/original_backup/sections/`, hash-verified).
- Deleted `assets/README.md` (exact duplicate of `templates/1/README.md`).
- `assets/noise_role_research.md` → `archive/` (superseded by `research/companies/Noise.md`).
- `assets/noise_cover_letter.md` → `archive/` (superseded by `applications/*/cover_letter.md` AND contains a boundary violation: "built a wearable AI pendant from scratch". Do not resurrect without rewriting).
- Kept all user PDFs untouched.

## Docs updated to match reality
- `README.md`, `agent.md`, `gemini.md` (mirrors — edited identically): all three tracks materialized as `_base.md`; tailored-output convention; fixed `resume_startup.md` references; dropped "create MNC/midlevel templates" next step.
- `CONTEXT.md`: fixed folder layout (`mid-level/` → `midlevel/`, dropped nonexistent `latex/`, templates no longer "empty", assets = PDFs only).

## Validation
- `versions/{startup,mnc,midlevel}/_base.md` all present (pipeline contract intact).
- Grep for `resume_startup|boundaries.yaml|assets/sections|2.zip` in active paths returns only historical handoff mentions.
- Prior deep-iteration content (facts.yaml 6-bullet startup, adapted backend, evidence.md enrichments) untouched and verified present.

## Addendum — deferred cuts applied (same day, user-approved)
- `gemini.md` → 4-line pointer to `agent.md` (were byte-identical 121-line mirrors; zero references to either filename). `agent.md` carries a one-line mirror note. Edit guidance in `agent.md` only.
- `templates/1/` → `templates/v1-dash-titles/`; `templates/2/` → `templates/v2-comma-titles/` (zero path references; `templates/README.md` rewritten with new names + which-is-which).
- Deleted `archive/bing_search_results.json`, `archive/search_cache.json`, `archive/repos.json` (unreferenced regenerable caches).
- `applications/Noise_2026-09/` → `applications/Noise_Eng_2026-09/` (symmetric with `Noise_PM_2026-09/`). Updated self-refs in its `pipeline_state.md` (2 lines) and the "do not touch" note in the PM `input.md`. File contents otherwise untouched per the do-not-touch rule. `archive/pipeline_old/PIPELINE_spec_v1.md` still cites the old name as a historical example — left as archive history.

## Addendum — Ousterhout redesign pass (same day)
Method: *A Philosophy of Software Design* red flags + principles (deep modules, info hiding, C=Σcp·tp weighted by touch frequency). Context preserved: facts.yaml, tracks, truth boundaries, pipeline behavior all untouched.
- Iter 1 (Repetition, highest touch weight): `agent.md` Truth Boundaries + Correct Attribution + Writing Style (24 lines duplicating `boundaries.md`, verified superset before removal) → pointer to `content/github/boundaries.md`. Project Identity → pointer to `CONTEXT.md`. `README.md` Maintenance (duplicating agent.md workflows) → pointer to agent.md as single owner. Fixed rename drift: README + CONTEXT now say `v1-dash-titles`/`v2-comma-titles`. agent.md 5607→4712 bytes.
- Iter 2 (obscurity): deleted empty `research/job_application/{application_tactics,linkedin_profile}/` (zero refs, zero bytes of content).
- Iter 3 (unknown unknowns): `DONT.MD` header gains research-provenance pointer to `youtube-resume-videos-report.md`; README handoff pointer corrected to `resume_iteration_2026-09-14.md` as current (old `resume_handoff.md` marked superseded, unedited).
- Deliberately NOT changed: `templates/` duplication (standing keep-both decision, merge risks builds); `DONT.MD` caps name (Ch.17.3 — consistency taken too far); root checklist PDF (4 inbound path refs, move = change amplification); pipeline stages (each deep, own abstraction); `facts.yaml` per-track keys (verified genuinely different); `crisil_ai_intern_brief.md` location (visible in 4-item `research/`, move = churn). Stop: no remaining change reduces understanding cost.

## Addendum — context-system architecture (same day, agent-behavior spec)
Audit found `DONT.MD` (1656 lines, canonical) had zero inbound refs from any active file; truth rules restated 4× (agent.md, stages 06/07, facts.yaml `boundaries:`); no `AGENTS.md`; no `memory/`; no research index; stale pointers (handoff "current", project count 4 vs 3, DOCX claim).
- CREATED `AGENTS.md` (behavior, load policy, ownership, precedence, maintenance, learning loop, dependency note), `memory/lessons.md` (7 seeded evidence-backed lessons), `research/README.md` (per-file purpose + load-when).
- `agent.md` → pointer to `AGENTS.md` (compat); `gemini.md` → pointer to `AGENTS.md`.
- `CONTEXT.md`: full folder map (added data/pipeline/applications/research/skills/memory); MNC "PDF + DOCX" → "PDF" (no docx exists).
- `README.md`: projects 4 → 3; added `.agents/skills` tree line; workflows already point at single owner (retargeted to `AGENTS.md`).
- `data/facts.yaml`: `boundaries:` block (14 items, all verified present in `boundaries.md`) → pointer comment + `[]`; per-record `notes:` kept (data annotations). YAML validated.
- Stages 06/07: inline buzzword/metric/hard-rule restatements → canonical refs (`boundaries.md` full, `DONT.MD` §5/§12/§19). Both stages already load `boundaries.md`, so this strictly cuts per-run tokens.
- NOT touched: `DONT.MD` body (canonical), run traces, history, skills layout, templates, archive. Known frozen cross-refs now pointing at moved text (`research/resume_best_practices.md:822`, PM `pipeline_state.md:257` citing old `facts.yaml boundaries`) left as history; anchors survive in `boundaries.md`/`evidence.md`.
- Validation: single ownership confirmed by grep; zero stale names; YAML parses; all `AGENTS.md` targets exist; DONT.MD cited sections exist (bare-header style: `5.`, `12.`, `19.`); no load cycles (AGENTS→CONTEXT one-way; CONTEXT mentions are discovery only).

## Addendum — JD-to-application entry point (same day)
- New `pipeline/APPLY.md`: fill-INPUT-and-send run prompt (Understand → Research → Match → Ask → Tailor → Validate → Prepare → Learn). Extends `run.md` Stages 01–10 with `interview_prep.md` (new per-run artifact, schema defined in APPLY.md) + learning step.
- Review fix: ASK step checks prior runs' `input.md` notes only — never `pipeline_state.md` audit traces (consistent with AGENTS.md NEVER rule).
- Discovery: `pipeline/README.md` quick-start + tree entry, root README tree line, `AGENTS.md` maintenance line all point to APPLY.md.

## Addendum — no-ai-slop stability audit (same day)
- Verified external Skill B (`no-ai-slop` SKILL.md 11KB + eval.md 43 lines): Edit/Detect modes, eval self-check loop, and all 17 detect patterns named by Stage 10 all exist. In-repo Skill A (`stop-slop` Quick Checks + rule 4 "be specific") verified — Stage 10's references are exact.
- Fixed 2 gaps in `pipeline/stages/10_slop_free_polish.md`: (1) banned-word list was a subset claiming to be the Skill B list — added missing "this is huge", "this changes everything"; (2) no preflight for the machine-global Skill B path — added step 0: verify all skill files exist, stop + surface if Skill B missing, never substitute Skill A for the cover letter.
- Terminality confirmed end-to-end: run.md Step 8 + gate, Stage 09 hands off READY_FOR_STAGE_10 only, APPLY.md step 6, pipeline/README design decision, memory lesson 5. No step-number cross-refs broken (renumber is internal 0–10).
